package com.ghostnexora.vpn.ui.theme

import androidx.compose.ui.unit.dp

// ══════════════════════════════════════════════════════════════════════════
// GHOST NEXORA VPN — DIMENSIONES Y ESPACIADO
// Sistema de espaciado consistente basado en múltiplos de 4dp
// ══════════════════════════════════════════════════════════════════════════

object Dimens {

    // ── Espaciado base ────────────────────────────────────────────────────
    val SpaceXXS  = 2.dp
    val SpaceXS   = 4.dp
    val SpaceSM   = 8.dp
    val SpaceMD   = 12.dp
    val SpaceLG   = 16.dp
    val SpaceXL   = 20.dp
    val SpaceXXL  = 24.dp
    val Space2XL  = 32.dp
    val Space3XL  = 40.dp
    val Space4XL  = 48.dp
    val Space5XL  = 64.dp

    // ── Padding de pantalla ───────────────────────────────────────────────
    val ScreenPadding         = 16.dp
    val ScreenPaddingLarge    = 24.dp
    val ContentPadding        = 12.dp

    // ── Tarjetas ──────────────────────────────────────────────────────────
    val CardPadding           = 16.dp
    val CardPaddingSmall      = 12.dp
    val CardElevation         = 0.dp   // Sin sombra; usamos bordes neon
    val CardBorderWidth       = 1.dp

    // ── Botón principal del Dashboard ─────────────────────────────────────
    val ActionButtonSize      = 120.dp
    val ActionButtonRing      = 8.dp   // Grosor del anillo exterior
    val ActionButtonIconSize  = 48.dp

    // ── Lista de perfiles ─────────────────────────────────────────────────
    val ProfileCardHeight     = 80.dp
    val ProfileIconSize       = 44.dp
    val ProfileCardSpacing    = 8.dp

    // ── Navigation Drawer ─────────────────────────────────────────────────
    val DrawerWidth           = 280.dp
    val DrawerItemHeight      = 52.dp
    val DrawerIconSize        = 22.dp
    val DrawerHeaderHeight    = 180.dp

    // ── Floating Window / Bubble ──────────────────────────────────────────
    val BubbleSize            = 56.dp
    val BubbleIconSize        = 28.dp
    val FloatingPanelWidth    = 240.dp
    val FloatingPanelHeight   = 160.dp

    // ── Top Bar ───────────────────────────────────────────────────────────
    val TopBarHeight          = 56.dp
    val TopBarIconSize        = 24.dp

    // ── Indicadores de estado ─────────────────────────────────────────────
    val StatusDotSize         = 10.dp
    val StatusRingSize        = 18.dp
    val StatusIconSize        = 64.dp

    // ── Logs ──────────────────────────────────────────────────────────────
    val LogEntryHeight        = 48.dp
    val LogLevelBadgeWidth    = 64.dp

    // ── Bordes ────────────────────────────────────────────────────────────
    val BorderThin            = 0.5.dp
    val BorderNormal          = 1.dp
    val BorderThick           = 1.5.dp
    val BorderAccent          = 2.dp

    // ── Glow / Sombra neon ────────────────────────────────────────────────
    val GlowRadius            = 24.dp
    val GlowRadiusLarge       = 48.dp

    // ── Iconos ────────────────────────────────────────────────────────────
    val IconXS                = 16.dp
    val IconSM                = 20.dp
    val IconMD                = 24.dp
    val IconLG                = 32.dp
    val IconXL                = 40.dp
}
